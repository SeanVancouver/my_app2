import faker from "faker";

// export default getProfileList;

// const resetModals = () => {};

// const mapStateToProps = (state) => {
//   return { ModalReducer: state.ModalReducer };
// };

// export default connect(mapStateToProps, { fetchUsers })(resetModals);
